name = "obsidian-sync-server"
main = "src/index.js"
compatibility_date = "2024-11-01"

# Gitee 仓库配置。GITEE_TOKEN 是机密，不要写在这里，
# 本地开发放到 .dev.vars（参考 .dev.vars.example），线上用 wrangler secret 配置。
[vars]
GITEE_OWNER = ""
GITEE_REPO = ""
