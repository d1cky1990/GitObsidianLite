# 依赖与构建产物
node_modules/
dist/
web/dist/
*.zip
*.exe
*.timestamp-*.mjs

# 密钥（严禁提交 / 上传到任何部署平台）
.dev.vars
.env
.deploy-token
*.token

# 本地私有目录（不进公开仓，也不上传任何部署平台）
#   .workbuddy/  工作记忆与日志——每次 AI 会话的私有上下文
#   .scratch/    未发布的草稿区；成熟的设计档案放 docs/design/
# 注意：凭据由上面的 .dev.vars / *.token 规则挡住，与这两个目录互不替代。
.workbuddy/
.scratch/
